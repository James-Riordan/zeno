[metadata]
name = zenocli
version = 0.1.0
description = Zeno CLI — control interface for engine, viewer, and orchestration
author = James (Zigadel)
license = MIT

[options]
packages = find:
install_requires =
    typer[all]

[options.entry_points]
console_scripts =
    zeno = zenocli.main:app
